{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "check-routes-testclient",
			"type": "shell",
			"command": "c:/Users/clyel/Desktop/CodingWeek/.venv/Scripts/python.exe",
			"args": [
				"-c",
				"from fastapi.testclient import TestClient; import app.app as m; c=TestClient(m.app); print('root', c.get('/').status_code); print('login', c.get('/login').status_code)"
			],
			"isBackground": false
		}
	]
}